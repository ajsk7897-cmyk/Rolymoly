# 롤 클랜관리 시스템 패치 노트 (최근 업데이트)

## 📌 [어제] 시스템 안정화 및 코어 로직 개편

1. **스코어 증감 로직 개선**
   - 내전 종료 후 승패에 따른 스코어(MMR 및 클랜 점수) 증감 로직을 정밀하게 수정 및 고도화했습니다.

2. **UI 최적화 및 안정성 패치**
   - Streamlit 캐시 충돌로 인해 간헐적으로 발생하던 `IndexError` 버그를 완벽히 수정했습니다.
   - 기본 UI와 충돌을 일으키던 불필요한 배경 설정(set_background) 호출을 제거하여 쾌적한 기본 UI로 롤백 및 안정화했습니다.

---

## 📌 [오늘] 일반내전(20인) 확장 및 내전 이력(K/D) 고도화

1. **'일반내전 (20인)' 전용 탭 신설 및 매칭 시스템 확장**
   - 기존 일반내전(10인)과 탭을 분리하여 **20인 전용 시스템**을 새롭게 추가했습니다.
   - 포지션 기반 자동 4팀 배정 기능과 수동 팀 변경(드롭다운) 기능을 완벽히 지원합니다.
   - 경매내전과 동일하게 **단판승부, 풀리그, 토너먼트, 조별리그** 등 다양한 게임 방식을 선택할 수 있습니다.

2. **내전 이력: 킬/데스 기록 및 득실차(K-D) 랭킹 시스템 도입**
   - **(풀리그 & 조별리그)** 각 매치 결과 저장 시 단순 승패뿐만 아니라 **승리 킬** 및 **승리 데스(패배 킬)**를 함께 기록할 수 있는 폼을 추가했습니다.
   - 각 팀의 승점이 동일할 경우, 기록된 누적 킬과 데스를 바탕으로 **득실차(K-D)가 높은 팀이 순위표 상단에 랭크되도록** 코어 정렬 로직을 개편했습니다.

3. **내전 이력: 조별리그 스케줄 라운드(Round) 뷰 개선**
   - 기존 A조/B조 묶음으로만 보여주던 조별리그 스케줄을, 진행 순서에 맞춰 **Round 1, Round 2** 형식으로 교차 렌더링되게 개선하여 직관성을 높였습니다.

4. **가시성 및 기타 버그 픽스 (Hotfix)**
   - **라이트 테마 가시성 패치**: 라이트 테마 사용 시 팀 이름이 흰색 글씨로 고정되어 보이지 않던 이슈를 기기 테마에 맞춰 자동 반전되도록(css var 수정) 해결했습니다.
   - **하위 호환성 에러 패치**: 킬/데스 기능이 도입되기 전 생성된 과거 데이터에 결과를 저장할 때 발생하던 `KeyError: 'kills'` 에러를 안전하게 예외 처리하여 수정했습니다.
